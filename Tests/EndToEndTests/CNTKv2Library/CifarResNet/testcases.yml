dataDir: .

tags:
    - bvt-e (build_sku == '1bitsgd') and ((flavor == 'release') ^ (os == 'linux'))
    - nightly-e (build_sku == '1bitsgd')

testCases:
  Test run must be completed:
    patterns:
      - "CNTKv2Library-CifarResNet tests: Passed"

